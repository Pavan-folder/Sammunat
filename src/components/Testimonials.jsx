export default function Testimonials(){
  return (
    <section className="testimonials">
      <div className="container">
        <h2>Testimonials</h2>
        <p style={{color:'var(--muted)'}}>"Great work — helped our product ship faster." — Client</p>
      </div>
    </section>
  )
}
